import React, {Component} from "react"

export default class CreateUser extends Component {
    render(){
        return(
            <div>
            <h1>This is the user creation menu, bro.</h1>
            </div>
        )
    }
}